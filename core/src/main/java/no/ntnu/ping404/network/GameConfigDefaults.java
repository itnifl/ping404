package no.ntnu.ping404.network;

public class GameConfigDefaults {
    public static final int DefaultWinScore = 5;

    /** Initial spawn X offset (pixels from left edge). */
    public static final float SPAWN_X_OFFSET = 100f;
    /** Random range added to SPAWN_X_OFFSET for the initial X position. */
    public static final float SPAWN_X_RANGE = 400f;
    /** Initial spawn Y offset (pixels from bottom edge). */
    public static final float SPAWN_Y_OFFSET = 100f;
    /** Random range added to SPAWN_Y_OFFSET for the initial Y position. */
    public static final float SPAWN_Y_RANGE = 400f;
}
